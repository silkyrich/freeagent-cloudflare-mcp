{
  "name": "freeagent-claude-connector",
  "version": "1.0.0",
  "description": "Remote MCP server (Cloudflare Worker) exposing FreeAgent to Claude via OAuth \u2014 reads + journalled, undoable writes",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.8.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.17.3",
    "hono": "^4.12.27",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260701.1",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "wrangler": "4.106.0"
  }
}